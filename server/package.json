{
  "name": "qlic-json-server",
  "version": "1.0.0",
  "main": "start.cmd",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}